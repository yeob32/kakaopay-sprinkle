spring:
  datasource:
    hikari:
      jdbc-url: jdbc:h2:./payDemo
      username: sa
      password:
      driver-class-name: org.h2.Driver
  h2:
    console:
      path: /h2-console
      enabled: true
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop